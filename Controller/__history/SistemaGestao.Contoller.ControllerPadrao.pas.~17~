unit SistemaGestao.Contoller.ControllerPadrao;

interface

        type

        TController<T:class> = class
        private

        protected

        public
        function salvar: string;virtual;
        function pesquisar (phave,ptipo:string):<T> virtual;
        procedure setDm  (pdm:TObject);  virtual;
        function carregar : TObject;virtual;
        published
          { published declarations }
        end;
implementation

{ TController }

function TController.carregar: TObject;
begin

end;

function TController.pesquisar(phave, ptipo: string):<T>;
begin

end;

function TController.salvar: string;
begin

end;

procedure TController.setDm(pdm: TObject);
begin

end;

end.
